# 코드 7-4

# 사이킷런의 LabelEncoder를 임포트한다.
from sklearn.preprocessing import LabelEncoder

# data의 'Species'열을 flower 객체에 저장한다.
flower = data['Species']

# LabelEncoder 객체를 만들어 fit, transform을 한 값을 Ftarget에 저장한다.
encoder = LabelEncoder()
encoder.fit(flower)
Ftarget = encoder.transform(flower)

# Ftarget을 출력한다.
print(Ftarget)
