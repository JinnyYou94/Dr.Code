# 코드 3-9

# 짝수와 홀수의 합을 저장할 변수 a, b에 0을 저장한다.
a = 0
b = 0

# i가 1부터 50까지 값이 증가할 때마다
for i in range (1, 51):

# 만약 i의 값을 2로 나눈 나머지가 0이라면 (짝수라면)
    if (i % 2 == 0):

# a와 i를 더해서 a에 값을 새로 저장한다.
        a = a + i

# a를 출력한다.
print('1부터 50까지 짝수의 합은 ' , a, '이다')

# 1부터 50까지 홀수의 합을 모두 더해 b를 출력한다.
for i in range (1, 51):
    if (i % 2 == 1):
        b = b + i
print('1부터 50까지 홀수의 합은 ' ,b, '이다')
