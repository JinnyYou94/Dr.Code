# 코드 4-8

# 넘파이를 임포트한다.
import numpy as np

# 넘파이의 arange() 함수를 사용해서 배열을 만들어 a에 저장한다.
a = np.arange(6)

# a를 출력한다.
print(‘[6] Shape \n’, a)

# a를 [2,3] Shape로 바꾸어 출력한다.
a = np.reshape(a, [2,3])
print(‘[2,3] Shape \n’, a)

# a를 [3,2] Shape로 바꾸어 출력한다.
a = np.reshape(a, [3,2])
print(‘[3,2] Shape \n’, a)
